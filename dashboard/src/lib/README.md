# dashboard/src/lib/

| File | What it is |
|---|---|
| `data.js` | fetches and shapes `dashboard/public/data/dashboard.json` and `actions.json` for the components to consume |
| `utils.ts` | shadcn's `cn` helper (`clsx` + `tailwind-merge`), used only by `components/ui/` |
