# assets/

Shared brand assets for the repo, used in `README.md` and (originally) copied into `site/` for
the standalone landing page.

| Path | What it is |
|---|---|
| `brand/` | the logo mark, full lockup, and README banner, see `brand/README.md` |
